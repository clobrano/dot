#xargs -a <(awk '/^\s*[^#]/' /path/to/this/file) -r -- sudo apt-get install

apt-file
clang-format
cmake
locate
net-tools
pandoc
pkg-config
ppa-purge
python-pip
python3-pip
rar
tree
unrar
vit
zathura
