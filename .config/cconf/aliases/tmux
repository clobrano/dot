tmux-try-attach () {
    if ! tmux list-sessions 2>&1 >/dev/null; then
        tmux new -s main
    else
        tmux attach
    fi
}
alias t="tmux-try-attach"
alias ta="tmux attach"
alias ts="tmux list-sessions"
