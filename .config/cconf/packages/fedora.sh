packages=(
    alacritty        # GPU-accelerated terminal emulator
    bat              # modern cat replacement with syntax highlighting
    bear             # compilation database generator for clangd
    calcurse
    clang            # C/C++ compiler frontend
    clang-tools-extra # clang-tidy, clang-format, and other clang tools
    curl             # command-line HTTP client
    delta
    eza              # modern ls replacement
    fd-find          # fast and user-friendly find alternative
    gh               # GitHub CLI
    git              # version control
    python3-pip      # Python package installer
    stow             # symlink manager for dotfiles
    tmux             # terminal multiplexer
    yq               # YAML/JSON processor (like jq for YAML)
    zoxide           # smarter cd command
    zsh              # Z shell
)
