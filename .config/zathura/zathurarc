set selection-clipboard clipboard
set page-padding 1
set window-width 1000
set window-height 800

map i recolor
map p print

# Map , (same button of <) to full page up
map , scroll full-up
# Map . (same button of >) to full page down
map . scroll full-down
